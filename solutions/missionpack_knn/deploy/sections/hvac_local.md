### After Deployment

1. Visit `http://localhost:8280`
2. Connect to OPC-UA server (or use built-in simulator for testing)
3. Upload historical data CSV, train prediction model
4. Configure parameter mappings, start auto-prediction
