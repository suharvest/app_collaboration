# 本机部署

将仓库管理系统部署到您的本地电脑。

## 前提条件

- Docker Desktop 已安装并运行
- 至少 2GB 可用磁盘空间
- 端口 2124 和 2125 可用

## 部署完成后

1. 打开 `http://localhost:2125`
2. 注册第一个管理员账户
3. 在 **用户管理** -> **API 密钥管理** 中创建 API 密钥
4. 复制 API 密钥（格式：`wh_xxxx...`）用于 MCP 配置
