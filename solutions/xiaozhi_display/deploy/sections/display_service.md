### After Deployment

1. Open `http://<device-ip>:8765` in display browser, press `F` for fullscreen
2. Wake up Watcher, say "Open settings"
3. Find "Display URL", speak the server address (e.g. `192.168.1.100:8765`)
4. Once connected, conversations will sync to the big screen
