# 本机部署

将语音服务部署到您的本地电脑。

## 前提条件

- Docker Desktop 已安装并运行
- reSpeaker XVF3800 已通过 USB 连接
- 至少 2GB 可用磁盘空间
- 端口 8090 可用

## 部署完成后

1. 执行 `docker ps` 确认容器运行中
2. 访问 `http://localhost:8090` 打开边缘客户端
3. 开始实时语音转录测试
