{
  "name": "provisioning-station-frontend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "tauri:dev": "cd ../src-tauri && cargo tauri dev",
    "tauri:build": "cd ../src-tauri && cargo tauri build"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.0",
    "autoprefixer": "^10.4.16",
    "jsdom": "^25.0.0",
    "postcss": "^8.4.32",
    "tailwindcss": "^3.4.0",
    "vite": "^5.0.10",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "marked": "^11.1.0"
  }
}
